import React from 'react';
import ContentContainerStyles from './styles';

const ContentWrapper = ({ children }) => {
  return <ContentContainerStyles>{children}</ContentContainerStyles>;
};

export default ContentWrapper;
